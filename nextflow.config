/*
 * Nextflow configuration for nf-core/ampliseq on NCHC Slurm HPC
 */

singularity {
    enabled     = true
    autoMounts  = true
    runOptions  = '-B /tmp:/tmp'
}

process {
    executor = 'local'
}
